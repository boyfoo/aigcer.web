# Public production origin, e.g. https://your-domain.example.
# Leave unset for local/private previews. Set before building for search indexing.
SITE_URL=

# Optional absolute path on a persistent volume. Defaults to web/storage/.
# Back up the SQLite database and uploads together; do not commit this directory.
JINGJIE_DATA_DIR=
