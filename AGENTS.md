# Web 项目协作约定

## 协作方式

- 产品方向或交互含义不明确时先讨论，一次问一个问题；已经确认的修改直接完成。
- 本文件只记录当前有效的长期约定。新决定替换旧规则，不累积过程记录、模板指令或源码已有的功能清单。

## 数据与构建

- 服务端内容由 `src/server/repository.js` 读取，`src/data.js` 仅用于首次初始化。
- 内容、发布快照、标签与上传素材由网站服务持久化，默认使用 `storage/` 下的 SQLite 和 uploads，可通过 `JINGJIE_DATA_DIR` 指向持久磁盘。
- 保持 `.openai/hosting.json`、`worker/index.js`、`scripts/prepare-sites-build.mjs` 与 `tests/sites-worker.test.mjs` 的既有职责及兼容性，不因界面调整替换项目结构。

## Node.js 与 npm 版本选择

- 执行前检查当前版本、`package.json` 的 `engines` 和 nvm 已安装版本，选择已安装且兼容的版本，不盲目选最高版本。
- 仅修改当前 PowerShell 进程的 `PATH`，Node.js 与 npm 必须来自同一版本目录；每次独立命令调用都重新设置并验证。
- 不执行 `nvm use`，不改共享链接、系统/用户环境变量或全局默认版本。npm 不可用时检查该目录的 `npm.cmd`，不为此更换包管理器。

具体步骤见 [README.md 的“PowerShell 临时选择 Node.js”](README.md#powershell-临时选择-nodejs)。

## 验证与命令执行

- 前端改动至少运行 `npm run build`；内容、标签与发布逻辑改动运行 `npm test`；涉及 Sites 构建或 Worker 时另跑 `npm run build:sites` 和 `npm run test:sites`。
- Sites 导出只包含已发布快照及其素材，不打包数据库或写入接口。
- 普通查询默认限时 30 秒、构建/测试 60 秒、依赖安装 180 秒。需延长时事先说明上限；工具的等待参数不是进程超时。
- 长命令记录会话或 PID，设置独立超时，每约 10 秒检查进展；约 20 秒无输出时诊断，不无期限等待。后台进程用 `Start-Process -PassThru -WindowStyle Hidden`，日志放入忽略目录。
- 超时先 Ctrl+C，5 秒未退出再核对并结束本次 PID 的进程树：`taskkill.exe /PID <本次PID> /T /F`。禁止按名称批量结束 Node.js 或其他项目服务；取消等待后也要确认子进程状态。
- 复用现有开发服务。新启动服务在 30 秒内确认就绪并记录 PID；不要重复启动或扫描端口。用户询问进度时先说明当前工作，每 60 秒内保持必要更新。

## 产品与数据边界

- 网站由本人积累与维护，供自己回顾和访客公开学习；当前浏览、录入和设置均免登录，不自行增加账号或权限流程。
- `/content` 与 `/settings` 是独立页面，入口在个人菜单，保存后留在当前页。录入支持本地上传素材、不完整草稿，以及在原表单内新增一级/二级标签；新增标签保留未保存输入并同步网站设置。
- 保存草稿与手动发布分开。发布需有素材且至少有提示词或分析之一；编辑已发布内容时，保存不改变公开快照，须明确发布更新。
- 已发布内容可下架并保留资料，不退回草稿；重新上架恢复原公开快照，不顺带发布待更新内容。只有从未发布的草稿可删除。
- `/cases/[slug]`、`/collections/[slug]` 使用稳定网址和可抓取链接，服务端初始 HTML 包含已发布内容；未知、草稿、下架网址返回 404。管理页、`/case-preview?id=…` 与字体对比页不参与搜索收录，生产域名由 `SITE_URL` 配置。
- “收藏案例”保存整条案例，“镜头收藏夹”整理具体镜头；访客收藏保存在当前浏览器，无账号同步。保留既有存储键、分组、备注和排序数据。
- 资料按真实录入展示，未知内容不推测、不生成模拟结论；缺尾帧不造替代帧，封面兜底须标明。质量检查区分结构校验与作者人工复核，资料或视频修改后清除旧复核确认。
- 离线报告选择本地原视频播放，不上传视频；缺失图片须明确提示，人物图片也纳入已发布素材导出。

## 页面与交互

- 主导航为“看案例、我的收藏”。分类筛选只保留一套页面内入口：桌面在左侧、窄屏在内容上方，默认收起且可就地展开；同组多选为 OR，不同组为 AND，选中条件可见且可清除。
- 案例页桌面左侧播放、右侧阅读，报告也排列在右侧。纵向滚动只使用页面根节点；隐藏滚动条时保留滚轮、触摸和键盘操作，横向裁切用 `overflow-x: clip`。
- 镜头列表只保留总览这一套。总览与单镜头细读共用播放器，细读使用上一镜、下一镜和全部镜头；返回总览保留筛选与浏览位置，切换镜头保持阅读位置。
- 单镜头按“标题与切换 → 当前画面 → 拆解正文 → 按需展开的参数/提示词”排列。画面常显、可放大，有尾帧时原位切换；切换镜头后回到首帧。
- “拆解跟随播放”默认关闭，开关和状态常显在右侧，不能藏入左侧工具。节奏带按真实镜长分配宽度，未拆解区间留空。
- 视频、播放状态和“播放与研究工具”一起固定；工具默认展开、可手动收起。固定时仅显示返回和案例名称的精简标题，标题独立占位，视频在固定前后宽高不变，不能挤压视频或推动右侧正文。
- 右侧顶部只保留一套“镜头拆解、整片统计、出场人物、质量检查、导出报告”固定导航，后接跟随开关和内容。报告默认展开、可收起，当前阅读区块对应导航为黄色选中态；点击跳转和窄屏换行按实际固定区高度避让，不能遮住标题。
- 录入页一次最多展开一个镜头，切换时保留所点标题的屏幕位置及已填内容；新增镜头在原新增按钮处展开，不播放长表单高度动画。
- 保留首页精选图片与常规结果列表。不要恢复播放器上方的“看成片”引导段、案例底部推荐、分类页顶部介绍或宣传横幅，也不留对应占位。

## 动画与恢复

- 必要的状态切换优先使用移动动画；进入、退出和恢复必须成对设计，恢复沿原路径反向移动，不能只给进入加动画、恢复时突然跳回。
- 完整标题与吸附标题互斥显示：完整标题先隐藏，再从上方滑入吸附标题；滚回时等吸附标题滑出完成，再恢复完整标题。延迟退出期间保持互斥，隐藏时保留原占位。
- 标题文字全程完全不透明，使用 `transform` 平移，不用淡入淡出或缩放替代移动；运动限制在独立标题区域，不改变页面高度。
- 过渡可在当前位置中断、平滑反向，快速来回操作不重播、不排队；无需动效的高频操作保持直接反馈。
- 隐藏内容不可聚焦或点击；键盘直达和系统“减少动态效果”模式直接切换。

## 视觉样式

- 全站共用一套深色主题变量，选中与交互强调色为 `#FCD535`，黄色实心按钮使用深色文字。
- 字体沿用 `Noto Sans SC Variable`，备用 `PingFang SC`、`Microsoft YaHei`、sans-serif；正文 400、标题以 500 为主，中文正文字距为 0。正文 16–17px、行高约 28–30px、阅读宽度上限 640px、段距 24px。
- 保持页面充分利用宽度，约束阅读区和图片尺寸，避免超宽屏拉伸；保留已有案例素材，不为局部修复重做布局或换字体。文案自然换行，不按单张截图硬塞换行符。
