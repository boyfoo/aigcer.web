# 镜界设计系统

> 更新：2026-09-24  
> 适用范围：镜界网站的访客页面、案例拆解、收藏、内容录入及设置。  
> 本文记录已确认的设计规则和当前实现基准，不是新的改版方案。

## 1. 产品与设计原则

镜界是围绕已有 AI 视频、图片结果的学习网站。核心路径为：

**观看结果 → 识别效果 → 阅读拆解 → 对比理解 → 收藏参考**

- 内容是主角。界面采用克制的深色背景、暖白文字和黄色强调，给视频与画面留出空间。
- 不提供视频生成、跟练、作业或作品提交。提示词用于阅读与复制，不做编辑和生成工作台。
- 保持页面简洁，避免重复引导、宣传横幅和无意义的提示占位。
- 状态必须可区分：可点击、悬停、选中、正在播放、正在阅读不能使用同一套表现。
- 局部调整优先复用现有组件和样式，不顺带改版、换字体或替换案例素材。
- 资料按真实内容展示；缺失内容不补造，封面替代镜头画面时明确说明。

## 2. 品牌与图标

### 2.1 品牌组合

正式名称为「镜界」，图标为黄色折角 J，带细深色轮廓。

| 项目 | 当前规范 |
| --- | --- |
| 左上角图标显示尺寸 | 32 × 32px |
| 「镜界」字号 | 26px |
| 品牌字重 | 500 |
| 图标与文字间距 | 8px |
| 品牌文字颜色 | `var(--text)` |
| 悬停、聚焦、按下 | 品牌文字保持原色；键盘焦点轮廓仍保留 |
| 点击行为 | 返回案例首页，遵循已有未保存内容拦截逻辑 |

图标使用真正带 Alpha 通道的透明 PNG；背景、J 内部留白和折角镂空均须透明。不得使用黑底、白底或棋盘格图片冒充透明。细深色描边用于在白色背景上保持轮廓，不增加独立方框或背景底板。

### 2.2 网站资产

只保留以下三个用途明确的运行时图标文件：

| 文件 | 用途 | 规格 |
| --- | --- | --- |
| [public/favicon.ico](public/favicon.ico) | 浏览器标签页 | 单文件内含 16、32、48px |
| [public/logo.png](public/logo.png) | 网站内品牌图标 | 512 × 512px 透明 PNG，按需缩小显示 |
| [public/apple-touch-icon.png](public/apple-touch-icon.png) | Apple 添加到主屏幕 | 180 × 180px |

- 不为每个显示尺寸重复添加 PNG 文件。
- 更换图标时同步更新页面引用的资源版本，避免旧缓存。
- 检查透明度时读取 Alpha 通道；不能只看黑色预览背景判断。
- 检查 favicon 时同时确认浏览器解码和页面引用，不能只检查文件存在。
- 常规功能图标沿用 `@ant-design/icons`，通过尺寸和颜色统一，不混入多套风格。

## 3. 色彩

### 3.1 全站语义颜色

优先使用 [src/styles.css](src/styles.css) 的变量，不在组件中另造近似色。

| CSS 变量 | 值 | 用途 |
| --- | --- | --- |
| `--page-bg` | `#0B0D0C` | 页面底色 |
| `--surface` | `#111411` | 容器、卡片底色 |
| `--surface-soft` | `#1B1D19` | 轻微层次、局部悬停背景 |
| `--text` | `#EDE7DC` | 主文字，暖白 |
| `--muted` | `#AAA69D` | 次要文字、参数与说明 |
| `--accent` | `#FCD535` | 选中、播放、收藏状态和重要操作 |
| `--accent-strong` | `#FFE36B` | 黄色操作的悬停强调 |
| `--line` | `#393A34` | 普通分隔线与容器边界 |
| `--line-strong` | `#565146` | 较清晰的中性边界 |
| `--tag-bg` | `#2B2A24` | 标签底色 |
| `--media-border` | `rgba(220, 201, 165, 0.33)` | 图片边界 |
| `--header-bg` | `rgba(11, 13, 12, 0.9)` | 顶栏底色 |

### 3.2 Ant Design 主题入口

组件库主题集中在 [src/Providers.jsx](src/Providers.jsx)，采用 `theme.darkAlgorithm`。

| 配置 | 输入值 |
| --- | --- |
| `colorPrimary` | `#FCD535` |
| `colorPrimaryHover` | `#FFE36B` |
| `colorPrimaryActive` | `#E0B920` |
| `colorLink` | `#EDE7DC` |
| `colorLinkHover` | `#F6F1E8` |
| `colorLinkActive` | `#EDE7DC` |
| `colorBorder` | `#494439` |
| `borderRadius` | 8px |
| `controlHeight` | 40px |
| `Button.lineWidth` | 2px |

这些是主题输入值；暗色算法可能派生实际颜色，验收时检查浏览器计算样式。

**禁止恢复组件库默认蓝色链接。** 已清除的默认色为 `#1668DC`、悬停色 `#15417E`、按下色 `#1554AD`。不要仅调整主色而遗漏独立的链接颜色配置。

### 3.3 颜色的状态含义

- 普通链接：保持文字色或悬停时轻微提亮。
- 已有黄色强调入口，例如「看视频与拆解」：悬停、按下保持黄色。
- 品牌文字：所有交互状态保持原色。
- 导航、工具、选中项原有的黄色反馈可以保留；禁止默认蓝色不等于禁止所有黄色悬停。
- 当前播放、已收藏和选中栏目继续使用黄色，并配合文字、图标或语义属性说明状态。
- 黄色实心按钮使用深色文字。
- 错误、危险操作、禁用状态继续使用相应语义样式，不全部改成黄色。

## 4. 字体与排版

字体栈：

```css
font-family: "Noto Sans SC Variable", "PingFang SC", "Microsoft YaHei", sans-serif;
```

| 层级 | 当前基准 |
| --- | --- |
| 阅读正文 | 16–17px，字重 400 |
| 阅读行高 | 约 28–30px；正文变量 1.75 |
| 阅读宽度 | 最大 640px / 40rem |
| 正文段距 | 24px |
| 常规标题 | 字重以 500 为主 |
| 视频案例标题 | 24px；吸附后 18px |
| 首页精选案例标题 | 22px；窄屏 20px |
| 常规案例卡片标题 | 18px；窄屏 16px |
| 主导航 | 16px；窄屏 14px |
| 辅助信息 | 通常 12–14px，使用次要文字色 |
| 品牌名称 | 26px，见品牌规范 |

- 中文正文不增加字距，使用自然换行。
- 当前标题允许轻微紧缩字距；不要把标题设置套用到正文。
- 时间、镜号等数字信息使用等宽数字特性，降低数值变化造成的跳动。
- 不根据单张截图手动插入换行来固定排版。
- 很小的字号仅用于镜头标签、时间轴等辅助内容，不用于主要阅读正文。

## 5. 边框、圆角与间距

### 5.1 边框

| 对象 | 规范 |
| --- | --- |
| 标准描边按钮 | 2px |
| 自定义黄色选中描边 | 2px，如首尾帧切换、录入列表选中项 |
| 返回圆形按钮 | 2px |
| 节奏带选中镜头外轮廓 | 2px |
| 键盘焦点轮廓 | 2px，保留可见间隔 |
| 普通分隔线、卡片及图片边界 | 通常 1px |
| 输入框、搜索容器 | 沿用现有局部边框，不因按钮加粗全局改成 2px |

描边加粗时应保持控件外部尺寸稳定。自定义按钮需要配合调整内边距，并在默认状态预留相同宽度的边框，避免悬停或选中时跳动。

不要全局把 `lineWidth` 改为 2 而影响所有组件；当前是在 `Button` 组件配置内设置。

### 5.2 圆角

| 对象 | 规范 |
| --- | --- |
| 常规按钮 | 8px |
| 小按钮、首页收藏按钮、首尾帧切换 | 6px |
| 标注选项按钮 | 7px |
| 浮动提示容器 | 8px；内部小按钮 4px |
| 图片、卡片、播放器 | 沿用现有 6–10px，具体按组件 |
| 标签、倒计时细条 | 胶囊圆角，999px |
| 返回按钮、状态圆点 | 圆形，50% |

按钮应有轻微圆角，不随意改成完全直角或大胶囊。文字链接和页签没有独立按钮外框，不为它们强行添加底板。

### 5.3 间距

优先复用现有间距：紧密图文 6–8px，同组控件 8–12px，内容分组 16–24px。上述为复用参考，不要求把现有组件机械改成统一网格。

品牌间距固定为 8px；正文段距 24px；案例页双栏间距使用 `clamp(28px, 3vw, 48px)`。

## 6. 布局与响应式

- 主导航为「看案例、我的收藏」，录入与设置放在个人菜单。
- 分类筛选只保留一套：桌面左侧，窄屏内容上方，默认收起。
- 首页保留精选案例和常规案例结果。不要恢复分类页宣传横幅、案例底部推荐或播放器上方重复引导。
- 案例页桌面左侧播放、右侧阅读；整体分析也在右侧，不另开一套播放器。
- 案例主体宽度上限 1440px，桌面当前两侧至少留出约 32px。
- 页面充分利用屏幕，但正文、图片和视频仍有尺寸限制，避免超宽屏拉伸。
- 纵向滚动使用页面根节点。隐藏滚动条后仍保留滚轮、触摸和键盘滚动。
- 横向裁切使用 `overflow-x: clip`，避免意外创建影响吸附的滚动容器。

沿用当前组件断点，不增加仅为一张截图服务的断点：

| 断点 | 主要职责 |
| --- | --- |
| 1100px 及以下 | 首页案例网格收拢 |
| 900px 及以下 | 首页筛选移到上方、导航与留白调整 |
| 800px 及以下 | 案例页转为窄屏布局，取消桌面固定阅读结构 |
| 620px 及以下 | 首页手机排版、筛选和字号调整 |
| 480px 及以下 | 案例工具与阅读导航进一步紧凑排列 |

## 7. 镜头列表与播放状态

### 7.1 列表交互

整行可点击进入镜头详情；画面放大仍保留独立入口。列表与网格、搜索、筛选和排序功能保留，返回总览时恢复之前的筛选及浏览位置。

| 状态 | 视觉反馈 |
| --- | --- |
| 普通条目 | 标题暖白，辅助文字灰色 |
| 普通条目悬停 | 整行背景极轻微变亮，标题和辅助信息少量提亮，箭头轻微变亮 |
| 当前播放条目 | 镜号和标题黄色；播放时显示「正在播放」，暂停时显示「播放位置」 |
| 当前播放条目悬停 | 不叠加背景、标题、说明或箭头的悬停变化 |
| 键盘聚焦 | 显示 2px 焦点轮廓 |

当前悬停实现基准：

| 作用元素 | CSS 值 |
| --- | --- |
| 行背景 `background` | `color-mix(in srgb, var(--text) 2%, transparent)` |
| 标题 `color` | `color-mix(in srgb, var(--text) 55%, white)` |
| 辅助信息 `color` | `color-mix(in srgb, var(--muted) 70%, var(--text))` |
| 箭头 `color` | `color-mix(in srgb, var(--muted) 85%, var(--text))` |

仅对非当前播放条目启用，并限定在支持 hover 的设备。

当前播放条目用 `aria-current="true"` 标识。正在阅读的镜头和播放位置可以不同，不能把阅读选中态当成播放态。

### 7.2 播放与阅读同步

「拆解跟随播放」默认关闭，开关和状态常显在右侧。

| 用户操作 | 右侧为全部镜头列表 | 右侧为镜头详情 |
| --- | --- | --- |
| 手动调整左侧视频进度或点击节奏带 | 更新对应播放标识，保持列表模式 | 切换到对应镜头详情，不受跟随开关限制 |
| 视频自然连续播放 | 更新播放标识，保持列表模式 | 仅跟随开关打开时自动切换详情 |

- 节奏带宽度按真实镜长分配，未拆解时间留空。
- 单镜头阅读顺序：标题与切换 → 当前画面 → 拆解正文 → 按需展开的参数和提示词。
- 首帧/代表画面常显，尾帧存在时才提供切换，换镜头后回到首帧。
- 不恢复已经移除的案例页「案例看点」和「示例拆解 · 分镜资料与视频画面不对应」提示区；真实缺失素材或替代画面说明仍按实际情况处理。

## 8. 播放位置浮动提示

当操作触发播放位置与当前阅读镜头不一致的提示时：

- 以浮动提示呈现「查看播放位置的镜头 XX」，不恢复播放器下方的固定说明行。
- 靠近本次触发点击的位置，必须避开视频画面及视口边缘。
- 提示未关闭又发生新触发时，立即使用新点击位置，并重新开始计时。
- 支持手动关闭、Escape 关闭和 3 秒自动关闭。
- 宽度随内容收紧，当前最大宽度 260px，同时受视口可用宽度限制。
- 容器高度 40px、圆角 8px；内部关闭按钮保持可操作面积。
- 底部倒计时为 3px 黄色细条，两侧留 8px，透明度 0.65，端点为胶囊圆角。
- 倒计时使用宽度收缩，避免缩放造成端点尖锐。
- 自然连续播放不反复弹出此提示。
- 减少动态效果模式使用分步倒计时，不增加连续移动动画。

## 9. 吸附与动画

- 桌面播放器、播放状态和工具一起固定，固定前后视频尺寸不变。
- 返回与案例名称只渲染一套，移动同一组元素完成精简标题切换，原位保留占位。
- 吸附后本次浏览保持精简标题。不要滚回去时突然恢复大标题或制造来回抖动。
- 右侧阅读导航到顶后保持贴顶；不要跟随正文长度或容器底部退回原位。
- 正文、播放器和标题的占位关系保持稳定；不靠隐藏透明文字制造假布局。
- 当前标题吸附过渡为 180ms，缓动 `cubic-bezier(0.22, 1, 0.36, 1)`，平滑到位，不回弹。
- 高频操作直接反馈，不排队播放动画；录入页不播放长表单高度动画。
- 尊重 `prefers-reduced-motion`，键盘直达和减少动态效果模式直接切换。
- 不拦截滚轮模拟滚动，不用持续滚动监听把页面拉回固定位置。

## 10. 通用组件与可访问性

- 优先复用 Ant Design 和已有业务组件，不增加职责重复的组件库。
- 搜索、筛选、选择器、按钮的颜色统一来自主题和语义变量，避免局部重新引入默认蓝色。
- 筛选条件可见且可清除；同组条件为 OR，不同组为 AND。
- 使用真实链接和按钮：导航使用链接，执行操作使用按钮。
- 图标按钮提供可访问名称；装饰图标和品牌旁重复图像避免重复朗读。
- 保留 `aria-current`、`aria-pressed`、`aria-selected` 等状态语义，并配合可见状态反馈。
- 鼠标悬停不是唯一提示方式，触摸与键盘操作也能识别控件和当前状态。
- 隐藏内容不可聚焦、不可点击；焦点轮廓不得因移除 hover 颜色而一并消失。
- 禁用、加载、空结果、缺失素材和错误应有明确反馈，不把未知内容伪装成正常数据。
- 收藏案例和收藏镜头使用清楚、不同的文案；不混淆整条案例与单个镜头。
- 内容录入的保存草稿与发布为两个操作，不因视觉调整改变数据行为。

## 11. 实现入口与维护

| 文件 | 职责 |
| --- | --- |
| [src/Providers.jsx](src/Providers.jsx) | Ant Design 主题、链接色和按钮默认值 |
| [src/styles.css](src/styles.css) | 全站变量、品牌、导航、首页、通用样式 |
| [src/App.jsx](src/App.jsx) | 品牌组合与页面导航 |
| [src/VideoStudy.jsx](src/VideoStudy.jsx) | 播放/阅读同步、吸附和浮动提示行为 |
| [src/video-study.css](src/video-study.css) | 案例播放器、镜头列表、详情与提示样式 |
| [src/ShotOverview.jsx](src/ShotOverview.jsx) | 镜头总览、首尾帧和播放标识 |
| [src/study-report.css](src/study-report.css) | 整体分析与报告导航 |
| [src/reference-projects.css](src/reference-projects.css) | 镜头收藏夹 |
| [src/content-entry.css](src/content-entry.css) | 内容录入与选中案例样式 |
| [src/app/layout.jsx](src/app/layout.jsx) | 网站图标元数据 |

- 新设计决定确认后，更新本文件对应条目，替换过时规则，不追加相互矛盾的历史说明。
- CSS 与组件库 token 的同一语义颜色应同步维护。
- 现有局部样式可能覆盖通用配置，必须检查最终计算样式，不能只看主题对象或第一条 CSS 规则。
- 本文描述设计约定；协作、环境、数据与交付流程以 [AGENTS.md](AGENTS.md) 为准，运行方式见 [README.md](README.md)。
- 本文不是所有页面已通过无障碍认证或全部状态已完成实机验证的声明。

## 12. 交付检查

1. 确认品牌尺寸、字体、主题颜色与本文件一致。
2. 检查默认、hover、active、focus-visible、选中和禁用状态，尤其是默认蓝色是否回流。
3. 检查 2px 描边不会造成尺寸跳动，普通分隔线没有被一起加粗。
4. 检查圆角适度，列表悬停足够轻微，当前播放项不会叠加 hover 效果。
5. 案例交互分别验证列表模式、详情模式、手动跳转和自动播放。
6. 在桌面与相关窄屏断点检查换行、遮挡、吸附和触摸操作。
7. 更换图标时验证真实透明度、白底/深底和小尺寸效果。
8. 前端修改至少运行 `npm run build`；涉及数据或发布时按 `AGENTS.md` 补充相关测试。
9. 纯文档调整检查内容和链接即可，不为文档改动重复构建网站。

