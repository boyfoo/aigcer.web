# 视频案例页体验调整验收

日期：2026-09-22。范围：视频页右侧信息组织与阅读交互。

## 视觉依据与证据

- 方案图：`C:/Users/yao/.codex/generated_images/01a0c7b3-c28d-75f1-8db8-185c77eadf78/exec-03a34248-b3d2-4406-b57c-22f38bf69339.png`。
- 最终实现：`.cache/video-layout-qa/desktop-reference-size.png`。
- 页面：`http://127.0.0.1:5174/cases/night-cinema`。
- 方案图 1367 × 1152 px，其中顶部 67 px 为演示标题；有效页面区域 1367 × 1085 px。实现截图及 CSS viewport 均为 1367 × 1085，devicePixelRatio = 1。对比排除演示标题，不进行字体或图片比例的强制缩放。
- 状态：深色主题、镜头拆解、总览、筛选收起、首镜选中、跟随关闭、播放起点。
- 已在同一次视觉输入中打开方案图和实现截图进行全页对比；导航、列表标题、缩略图、摘要与辅助入口均可在原尺寸读清，不另做放大裁切。
- 额外证据：`desktop-final.png`（1280 × 720）、`statistics.png`、`quality-pending.png`、`detail-from-report.png`、`export.png`、`filters-preserved.png`，均在上述缓存目录内。
- 手机证据：`mobile-overview.png`、`mobile-list.png`、`mobile-quality.png`，CSS viewport / 截图均为 390 × 844，density = 1；页面 scrollWidth = 390，无横向溢出。临时 viewport 覆盖已恢复。

## 比较记录与修正

1. 首轮发现列表每镜包含较多重复操作文案，压缩效果不足。证据：`desktop-initial.png`。按 P2 处理：镜号与标题合并，时间与景别/运镜合并，摘要仅在总览截为一行，详情完整保留；时长和播放位置保留。复查证据：`desktop-final.png`、`desktop-reference-size.png`。
2. 交互复查补齐从镜头返回来源报告后的键盘焦点。点击返回后焦点落在 `study-statistics` 面板，保留来源阅读位置。
3. 最终对比没有待处理的 P0/P1/P2 问题。

## 五项视觉检查

| 检查面 | 结论 |
| --- | --- |
| 字体与排版 | 复用站点 Noto Sans SC 与既有正文层级；不照搬生成图放大的字体。标题、辅助说明、时间区分清楚；总览摘要截断，正文仍完整可读。 |
| 间距与布局 | 主导航两项、导出独立，报告按二级栏目显示。紧凑列表保留首尾帧、放大、时长；当前尺寸可看到全部五镜。窄屏控件可换行，固定区域按实际高度避让。 |
| 色彩与状态 | 复用深色变量与黄色强调色；选中导航、阅读镜头、播放位置和检查状态仍可辨认。 |
| 图片与素材 | 保留真实案例封面、镜头帧和原视频，未用生成图中的假素材替换。详情图片 contain 展示，放大入口可用，缺尾帧不伪造。 |
| 文案与内容 | 保留示例资料与视频不对应声明；未引入练习或生成工作台。统计、人物、质量检查和导出均有明确入口。 |

## 接受的差异

用户要求不能丢失功能，且已有项目约定优先：保留“收藏案例”“从第 1 镜开始”、镜长、画面标注和独立图片放大；跟随开关放在固定导航下方，确保细读时也能访问。标题、左侧播放器、全站字号与真实素材沿用现有页面，因此不宣称与生成图逐像素一致。手机继续保留可收起的播放与研究工具。

## 功能保留与实际验证

- 主导航与三个整体分析栏目切换：通过；非当前内容隐藏。
- 整片统计、最长镜头跳转、返回来源和键盘焦点：通过。
- 出场人物入口与未录入空状态：通过；人物数据、关联计算与筛选逻辑保留。
- 质量检查全部 15 项、状态筛选、关联镜头与返回质量检查：通过。
- 搜索、所有筛选及排序入口、列表/网格：通过；搜索“情绪”得到 3 / 5 镜，进入详情返回后搜索与网格状态保留。
- 收起筛选后仍显示已选条件，可逐项或全部清除。
- 首帧独立放大：通过；样例没有尾帧，显示禁用尾帧入口，没有补造图片。
- 提示词完整内容和分段入口保留；复制视频动态完整提示词成功，浏览器剪贴板核对通过。
- 点击镜头仍从该镜自动播放；跟随默认关闭，上一镜、下一镜、回看、收藏和标注入口保留。
- 历史 `#study-quality` 链接可打开质量检查。
- JSON 与离线 HTML 导出入口可用，离线导出运行后恢复可操作状态；导出生成逻辑未变，既有导出测试通过。浏览器下载事件捕获超时，未据此声称下载文件已落地。
- 本次样例未包含真实人物关联与尾帧，相关数据与导出保留由源代码核对和既有测试覆盖，未修改内容数据库造测试资料。

## 工程验证

- Node.js v22.22.2 / npm 10.9.7，仅本次进程 PATH 指向同一版本目录。
- `npm test` 全部通过，覆盖内容、收藏、发布、统计、人物筛选、质量检查与离线报告等既有测试。
- 最后一次 `npm run build` 成功，日志：`.cache/video-layout-qa/final-build.log`。
- `git diff --check` 通过。
- 浏览器 error / warn 日志为空。
- 未新增依赖、未部署、未变更素材或持久数据。

## 后续细节

无阻塞项。手机保留的播放器与研究工具占用一定高度，可通过现有收起操作扩大阅读空间；本轮未改变该已确认行为。

final result: passed
