import FontTest from "../../font-test.jsx";
import { pageMetadata } from "../../lib/seo.js";

export const metadata = pageMetadata({ title: "字体对比", path: "/font-test.html", index: false });
export default FontTest;
